const testobj = {
    a: 5,
    b: false,
};

fn add(a, b) {
    a + b
}

print(testobj)
print(add)
print(add(5, 6))

const bool = 6 > 5 && 5 > 4;
print(bool)

const str = "hello" + " world 3+5 " + not(bool);
print(str)